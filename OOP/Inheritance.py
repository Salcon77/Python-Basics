#INHERITENCE

# Inheritence to defining a new class with little or no modification to an existing class.
# The new class is called derived ( child) class and the one from which it inherits is called the base(or parent) class.
